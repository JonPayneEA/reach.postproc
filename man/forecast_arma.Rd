% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-arma.R
\name{forecast_arma}
\alias{forecast_arma}
\title{Evaluate a deterministic ARMA error recurrence}
\usage{
forecast_arma(
  parameters,
  initial_errors,
  residuals,
  ma_parameters = numeric(),
  previous_residuals = numeric()
)
}
\arguments{
\item{parameters}{An AR parameter set created by \code{\link[=ar_parameters]{ar_parameters()}},
\code{\link[=default_ar_parameters]{default_ar_parameters()}} or \code{\link[=ar_parameters_from_timescales]{ar_parameters_from_timescales()}}. The
coefficient order determines how many values \code{initial_errors} must contain.
Coefficients are stored internally in the Deltares convention.}

\item{initial_errors}{Numeric vector containing the error values immediately
before the first forecast step. Supply values \strong{newest first}. For AR(3),
use \code{c(error_t_minus_1, error_t_minus_2, error_t_minus_3)}. The vector must
have the same length as the AR order and must not contain missing values.}

\item{residuals}{Numeric vector of innovations to apply during the forecast.
One value is required for each output timestep. Use \code{0} where no new
innovation is applied. A unit impulse response uses \code{c(1, 0, 0, ...)}.}

\item{ma_parameters}{Numeric vector of moving-average coefficients, ordered
from the most recent residual to the oldest. An empty vector, the default,
evaluates an AR-only model. These are MA coefficients, not rolling-average
window weights.}

\item{previous_residuals}{Numeric vector containing residual innovations from
before the first forecast step, supplied newest first. Its length must equal
\code{length(ma_parameters)}. Use \code{numeric(length(ma_parameters))} when no earlier
innovations should contribute.}
}
\value{
A \code{data.table} with one row per forecast step and columns containing
the AR contribution, current residual contribution, MA contribution and
resulting ARMA error.
}
\description{
Calculate an error series from autoregressive memory, current residual
innovations and moving-average memory. This function evaluates a supplied
ARMA model; it does not estimate AR or MA parameters from observations.
}
\examples{
parameters <- default_ar_parameters()

innovations <- c(1, rep(0, 23))

arma_forecast <- forecast_arma(
  parameters = parameters,
  initial_errors = c(0, 0, 0),
  residuals = innovations,
  ma_parameters = c(-0.5, -0.25),
  previous_residuals = c(0, 0)
)

head(arma_forecast)
}
