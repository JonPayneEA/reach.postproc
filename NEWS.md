# reach.postproc 0.7.5

- Put every roxygen tag on its own documentation line.
- Replaced malformed `@inheritParams` directives with explicit parameter documentation.
- Prevented roxygen2 from treating `@returns` and `@export` text as part of inherited topic names.

# reach.postproc 0.7.4

- Consolidated all check fixes.
